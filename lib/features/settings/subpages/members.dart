import 'package:flutter/material.dart';

class MembersFragment extends StatelessWidget {
  const MembersFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
